from app import app

if __name__ == "__main__":
    app.run(debug=True)
# This file is used to run the Flask application.
# It imports the app instance from the app module and runs it with debug mode enabled.